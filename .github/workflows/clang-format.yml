name: Clang format

on:
  push:
    branches:
    - main

jobs:
  format:
    runs-on: ubuntu-latest

    steps:
    - name: clang-format action
      uses: purduesigbots/clang-format-action@1.0.2
      with:
        style: WebKit
