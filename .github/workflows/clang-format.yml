name: Clang format

on:
  push:
    branches:
    - main

jobs:
  format:
    runs-on: ubuntu-latest

    steps:
    - name: clang-format Code Formatter
      uses: IvanLudvig/clang-format-action@v1.0
      with:
        myInput: world
